# Núcleo
pandas>=2.0
matplotlib>=3.7
pyarrow>=12.0          # leitura/escrita de parquet (cache)
unidecode>=1.3         # normalização de acentos na busca por nome

# Fonte principal: Base dos Dados (BigQuery)
basedosdados>=2.0

# Interface web (opcional)
streamlit>=1.30

# Exportação XLSX (opcional; CSV não requer nada extra)
openpyxl>=3.1

# Fonte alternativa sem BigQuery (opcional)
# pysus>=0.13         # SINASC/SIM via DATASUS
